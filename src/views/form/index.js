import TextField     from "./fields/text"
import PasswordField from "./fields/password"

// TODO: Déplacer les *Field vers `components/` si on arrive à les composer sans CSS!

export {
	TextField,
	PasswordField
}
