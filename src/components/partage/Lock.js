import React, { COmponent } from "react"

export default class Lock extends Component {
    constructor(props) {
        super(props)
        this.state = {
            actif: props.actif
        }
    }


}
