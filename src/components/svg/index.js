import fs from "fs"
import path from "path"

export function svgFileAsReactComponent(path) {
	console.error(
		"UNIMPLEMENTED",
		"La fonction de chargement de SVG n'est pas implémentée!",
		path
	)

	throw new Error("UNIMPLEMENTED: SVG to React component not implemented")
}
